# Le prompt à coller dans Claude Code pour être guidé pas à pas

Sur le serveur OpenClaw : exporter d'abord les variables de `config/env.example` dans le shell (au minimum `NOVIA_TELEGRAM_BOT_TOKEN`), puis ouvrir Claude Code dans le dossier du dépôt cloné (`cd immo9-novia-com && claude`) et coller le bloc ci-dessous tel quel. Claude Code hérite de l'environnement du shell qui le lance. Claude lit le dépôt, vérifie l'environnement, pose ses questions, propose chaque commande, attend votre accord avant toute écriture, et ne touche à rien d'autre que ce projet.

```text
Tu es mon assistant d'installation pour « Novia Com », un agent OpenClaw livré dans ce dépôt. Je suis l'administrateur technique d'IMMO9. Nous sommes sur le serveur OpenClaw d'IMMO9, dans le dossier du dépôt.

Règles absolues :
1. Tu lis d'abord README.md, docs/INSTALLATION.md, docs/SECURITE.md, docs/LIMITES.md et connectors/README.md en entier avant de proposer quoi que ce soit.
2. Tu n'exécutes aucune commande qui écrit (install.sh --apply, openclaw config patch, openclaw agents add, cron add, redémarrage du gateway) sans me l'avoir montrée et sans mon « ok » explicite. Les commandes de lecture (doctor.sh, openclaw --version, openclaw agents list, openclaw channels list, openclaw config validate, openclaw cron list --all, cat, ls, df, free) sont libres.
3. Tu ne modifies aucun fichier du dépôt sauf workspace/state/approvers.json, workspace/state/channels.json, workspace/templates/_tokens.json et les fichiers que docs/INSTALLATION.md te demande de remplir. Jamais AGENTS.md, SOUL.md, doctrine/, skills/, scripts/.
4. Tu ne touches pas aux autres agents, comptes ou crons de cette installation OpenClaw. Les seules modifications hors de l'agent, annoncées à l'avance, sont : l'installation des skills de skills-shared/ dans le dossier managé (visibles par tous les agents), cron.skipMissedJobs: true, et les entrées skills.entries.novia-* de la configuration (réglages des skills de cet agent, sans effet sur les autres). Tu n'installes pas de skill tiers non listé dans skills-shared/INSTALL.md.
5. Tu n'affiches, ne copies et n'enregistres jamais un secret (jeton Telegram, clés d'API). Quand une variable est nécessaire, tu me dis son nom et où la mettre (config/env.example), et tu vérifies seulement sa présence (test -n "$VARIABLE").
6. Si une commande échoue, tu montres l'erreur exacte, tu proposes au plus deux hypothèses, et tu n'essaies pas de contourner un verrou (approbations, onboarding, politique d'outils) : ils sont voulus.
7. Tu ne passes jamais à l'étape suivante sans avoir posé les questions de l'étape en cours et obtenu mes réponses. Une question à la fois quand la réponse conditionne la suite ; sinon, une liste courte que je remplis d'un coup.

Étape 0 · Vérifications préalables (lecture seule, avant toute proposition). Exécute et note le résultat de chacune :
a) Système : uname -a, df -h sur le dossier courant (au moins 2 Go libres), free -h ou équivalent (au moins 2 Go de RAM disponibles pour le rendu), date et fuseau (timedatectl ou date), utilisateur courant (whoami) et propriétaire des fichiers OpenClaw.
b) OpenClaw : openclaw --version. Requis : 2026.9.1 ou plus récent (l'installateur utilise config patch --replace-path et cron.skipMissedJobs). openclaw doctor et openclaw daemon status : le gateway tourne-t-il, sous quel utilisateur, avec quel fichier d'environnement ? openclaw config file : chemin de la configuration et taille de sa sauvegarde à prévoir.
c) Existant à ne pas casser : openclaw agents list --bindings (y a-t-il déjà un agent novia-com ou un workspace au même chemin ?), openclaw channels list (combien de comptes Telegram, lequel est le compte par défaut ?), openclaw cron list --all (combien de jobs, y a-t-il déjà des jobs « Novia » ?), openclaw skills list (skills déjà présents portant les mêmes noms que skills-shared/), et présence d'un plugin mémoire actif (memory-rag, GBrain, memory-core) dans openclaw config get plugins.
d) Outils : python3 --version (3.8 minimum), ffmpeg -version et ffprobe (déclinaisons, audiograms ; vérifie si le filtre drawtext est présent : ffmpeg -filters | grep drawtext), chromium ou google-chrome (rendu des carrousels et infographies), node --version (18 minimum, pour HyperFrames et supermonteur), git --version, curl.
e) Réseau : curl -sI --max-time 10 sur https://bofip.impots.gouv.fr/rss.xml, https://fpifrance.fr/rss.xml, https://www.journaldelagence.com/feed, https://api.telegram.org et https://api.upload-post.com (le serveur doit joindre les flux de veille et les API de publication).
f) Dépôt : bash scripts/doctor.sh, puis bash scripts/acceptance.sh (tests hors ligne : aucune modification du dépôt, écritures dans une copie temporaire ; doit finir par « 0 échoués »). Vérifie que workspace/state/approvers.json et channels.json n'existent pas encore ou ne contiennent plus les identifiants d'exemple.
Rends un tableau de compatibilité : vert (ok), orange (à corriger avant d'installer), rouge (bloquant), avec pour chaque orange ou rouge la commande ou l'action qui le règle. Si OpenClaw est plus ancien que 2026.9.1, propose la mise à jour selon la procédure habituelle d'IMMO9 et arrête-toi là tant qu'elle n'est pas faite. Si un agent novia-com ou un workspace identique existe déjà, arrête-toi et demande-moi quoi faire.

Étape 0 bis · Questions, à partir de ce que tu as trouvé (pose seulement celles qui ne sont pas déjà tranchées par les vérifications) :
1. Version et mise à jour : si la version est trop ancienne, quand et comment IMMO9 met à jour OpenClaw (fenêtre, sauvegarde, test sur un profil de développement) ?
2. Telegram : le bot « Novia Com » existe-t-il déjà ? Le groupe de travail existe-t-il ? Qui sont les personnes autorisées à valider (« Go », « Go publie ») et qui est administrateur ? Le compte Telegram par défaut du serveur doit-il rester celui qui existe (réponse attendue : oui) ?
3. Modèles : quels fournisseur et modèle utilisent vos agents (agents.defaults.model) ? Voulez-vous que Novia Com hérite du défaut ou un modèle imposé ? Quelle voie de génération d'images est disponible (outil image d'OpenClaw, Gemini, aucune pour l'instant) ?
4. Publication : choix du connecteur pour démarrer, upload-post.com (une clé, aucune revue d'application) ou API natives (Meta, LinkedIn, YouTube : applications à enregistrer et jetons à gérer) ? Quels comptes sont concernés (page LinkedIn, Instagram professionnel lié à une page Facebook, chaîne YouTube, TikTok) ? Qui détient les accès ?
5. Newsletter : outil d'emailing en place (Brevo ou autre), listes existantes par audience, expéditeur vérifié ? Si ce n'est pas Brevo, on démarre en brouillon manuel.
6. Mémoire : voulez-vous brancher Novia Com sur votre couche mémoire (memory-rag ou GBrain) dès la v1, ou garder la mémoire de fichiers du workspace pour commencer (recommandé) ? Si branchement : quelle source, avec quelle garantie qu'aucune donnée client n'y figure ?
7. Rendu : le serveur doit-il rendre les visuels et vidéos (chromium, node, ffmpeg installés) ou préférez-vous déporter les rendus lourds sur une autre machine plus tard ?
8. Métriques : comment l'équipe récupère-t-elle aujourd'hui les statistiques des plateformes (exports CSV, outil tiers) ? Qui déposera les exports dans outbox/_metrics/ ?
9. Coûts : plafonds de dépense par pièce (2 € par défaut) et par mois (60 € par défaut) à confirmer avec David ; quelles clés payantes sont acceptées (Gemini, ElevenLabs) ?
10. Périmètre : les cinq villes (Toulouse, Bordeaux, Montpellier, Nantes, Rennes) et les quatre audiences (primo-accédants, investisseurs, promoteurs, partenaires) sont-elles les bonnes ? Y a-t-il un sujet ou un partenaire à ne jamais nommer dès maintenant ?
11. Charte : le dossier de charte (logo, couleurs, typographies) est-il disponible pour l'onboarding (acte 3) ?
Consigne mes réponses dans un fichier docs/REPONSES-INSTALLATION.md (le seul fichier que tu crées), sans secret.

Déroulé, une étape à la fois, avec un récapitulatif d'une ligne à la fin de chaque étape :
Étape 1 · Telegram : guide-moi pour créer le bot avec @BotFather si besoin, le groupe « Novia Com », récupérer l'identifiant du groupe et ceux des personnes autorisées (openclaw directory ou @userinfobot). Aide-moi à remplir workspace/state/approvers.json et workspace/state/channels.json à partir des exemples (adaptateurs selon mes réponses de l'étape 0 bis), puis valide le JSON.
Étape 2 · Variables : vérifie par leur nom seulement que les variables de config/env.example indispensables au palier 1 sont présentes dans cet environnement ; dis-moi lesquelles attendent un connecteur. Si le jeton Telegram manque, arrête-toi et dis-moi comment le déclarer dans l'environnement du service gateway et dans ce shell avant de relancer Claude Code.
Étape 3 · Installation : montre-moi la sortie de bash scripts/install.sh (simulation). Explique chaque action prévue, y compris les modifications hors de l'agent. Après mon ok, exécute bash scripts/install.sh --apply. Vérifie ensuite openclaw agents list --bindings, openclaw config validate, openclaw skills check --agent novia-com, puis openclaw daemon restart et openclaw doctor. Si la configuration signale plusieurs comptes Telegram, propose openclaw config set channels.telegram.defaultAccount <compte existant>.
Étape 4 · Premier contact : envoie-moi le premier message à écrire à l'agent dans Telegram (« Commençons l'onboarding ») et vérifie dans les journaux du gateway que l'agent répond depuis le bon compte et le bon workspace.
Étape 5 · Onboarding : explique-moi les cinq actes de workspace/skills/novia-onboarding/SKILL.md et ce que l'équipe doit préparer (accès au site et aux comptes pour extraire la voix, charte, logos, salons, concurrents, presse locale, alertes Google en RSS). Reste disponible pendant l'onboarding pour vérifier les fichiers écrits par l'agent (doctrine/VOICE.md, LINES.md, templates/_tokens.json) et python3 workspace/skills/novia-onboarding/scripts/onboarding_state.py --check.
Étape 6 · Crons et test : après l'onboarding (je te le dirai), exécute bash crons/install-crons.sh --enable-p1 et openclaw cron list --all --agent novia-com ; puis guide-moi pour une première publication : python3 workspace/skills/novia-publish/scripts/publish.py <id> --preview (aperçu sans approbation ni envoi), puis « Go publie » dans Telegram en réponse à la carte, puis vérification sur la plateforme.
Étape 7 · Bilan : rends un tableau de ce qui est installé, de ce qui reste à brancher (connecteurs, mémoire, rendu), des points orange restants, et la commande de mise à jour hebdomadaire (bash scripts/update.sh).

Commence par l'étape 0.
```
